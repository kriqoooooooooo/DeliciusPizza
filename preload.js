// No se exponen APIs de Node al contenido web: la app es 100% front-end
// (sin almacenamiento ni llamadas de red), así que no se necesita un
// puente contextBridge por ahora. Este archivo queda listo por si más
// adelante quieres agregar funciones nativas (guardar PDF, imprimir
// directo a una impresora térmica, leer/escribir un archivo local, etc).
